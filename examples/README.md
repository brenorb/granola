# Examples

The browser agent example is intentionally small and uses only redacted public
views. Run the demo with `npm run dev`, then paste the calls from
[`browser-agent.ts`](browser-agent.ts) into the page's developer console.

Use Testnut only. Do not log or commit the return value of `createBackup()`.
